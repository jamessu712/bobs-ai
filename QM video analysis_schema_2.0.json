{
	"fieldSchema": {
		"fields": {
			"ShoppingCart": {
				"type": "string",
				"method": "generate",
				"description": "Obtain the shopping cart number that is a 10-digit number prefixed with #."
			},
			"PlaceOrderwith": {
				"type": "string",
				"method": "generate",
				"description": "Capture the text on the button at the bottom of the (Review Your Order) page (Place Order with xxx)"
			}
		},
		"definitions": {}
	}
}