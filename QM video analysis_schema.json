{
	"fieldSchema": {
		"fields": {
			"ShoppingCart": {
				"type": "string",
				"method": "generate",
				"description": "Shopping Cart #"
			},
			"PlaceOrderwith": {
				"type": "string",
				"method": "generate",
				"description": "Capture the text on the button at the bottom of the (Review Your Order) page (Place Order with xxx)"
			}
		},
		"definitions": {}
	}
}
